// Generated by LiveScript 1.4.0
'use strict';
var curry;
curry = require('./curry');
exports.even = function(x){
  return x % 2 === 0;
};
exports.odd = function(x){
  return x % 2 !== 0;
};
exports.random = curry(function(min, max){
  return min + Math.floor(Math.random() * (max - min + 1));
});
exports.range = curry(1, function(a, b, inc){
  var i$, to$, x, results$ = [];
  inc == null && (inc = 1);
  for (i$ = a, to$ = b + 1; inc < 0 ? i$ > to$ : i$ < to$; i$ += inc) {
    x = i$;
    results$.push(x);
  }
  return results$;
});
exports.gcd = curry(function(x, y){
  var z;
  x = Math.abs(x);
  y = Math.abs(y);
  while (y !== 0) {
    z = x % y;
    x = y;
    y = z;
  }
  return x;
});
exports.lcm = curry(function(x, y){
  return Math.abs(Math.floor(x / exports.gcd(x, y) * y));
});